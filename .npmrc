# Enforce Node/pnpm engine constraints declared in package.json
engine-strict=true

# Internal workspace dependencies are referenced as workspace:* (always latest in-repo)
save-workspace-protocol=rolling

# Automatically install missing peer dependencies
auto-install-peers=true

# Loosen peer-dependency strictness for ecosystem compatibility (e.g. Next.js plugins)
strict-peer-dependencies=false

